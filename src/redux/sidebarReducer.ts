type InitialStateType = {}

const initialState:InitialStateType = {}

export const sidebarReducer = (state: InitialStateType = initialState, action: () => void):InitialStateType => {

    return state

}